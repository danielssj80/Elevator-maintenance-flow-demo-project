# Offline-only dependencies for backend/ml/train.py and backend/ml/generate_predictions.py.
# NOT installed in the production Docker image or CI: nothing under backend/app/ imports
# these — the runtime container only reads the committed backend/ml/predictions.json.
#
# Usage: pip install -r requirements.txt -r requirements-ml.txt

xgboost>=2.0
shap>=0.45
joblib>=1.3
